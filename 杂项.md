# 杂项

## 代理修改

在powershell执行

```
reg query "HKCU\Software\Microsoft\Windows\CurrentVersion\Internet Settings" | findstr ProxyServer
```

可找到 Windows IP 地址 和 代理端口

```
export http_proxy=http://<Windows_IP>:<代理端口>
export https_proxy=http://<Windows_IP>:<代理端口>
```

可修改代理